# Merkle proof generation demo

This demo showcases the merkle proof generation. The proof is generated based on the input memory page cache and a multiproof.

## Quickstart

To execute tests, run `make test`.
